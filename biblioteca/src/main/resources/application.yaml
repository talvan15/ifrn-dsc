spring:
  application:
    name: biblioteca

  datasource:
    url: jdbc:postgresql://localhost:5432/biblioteca
    username:
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true